# Grafana가 뜰 때 Prometheus를 자동으로 데이터소스로 등록한다.
# (UI에서 수동으로 추가할 필요 없음)
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
