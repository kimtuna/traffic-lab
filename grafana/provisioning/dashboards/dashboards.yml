# /var/lib/grafana/dashboards 안의 JSON을 자동으로 대시보드로 등록한다.
apiVersion: 1

providers:
  - name: traffic-lab
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
